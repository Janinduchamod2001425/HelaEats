apiVersion: apps/v1
kind: Deployment
metadata:
  name: restaurant-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: restaurant-service
  template:
    metadata:
      labels:
        app: restaurant-service
    spec:
      containers:
        - name: restaurant-service
          image: restaurant-service:latest
          ports:
            - containerPort: 5005
          env:
            - name: MONGODB_URL
              value: "mongodb+srv://duvidukavin:q1pFAjX1A1BcsbVT@food-system-restaurent.9ack8du.mongodb.net/?retryWrites=true&w=majority&appName=food-system-restaurent"
            - name: PORT
              value: "5005"
            - name: NODE_ENV
              value: "production"
